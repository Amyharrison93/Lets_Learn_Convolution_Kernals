[project]
name = "my-package"
version = "0.1.0"
description = "Short description"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["__testing__"]
python_files = ["test_*.py"]